# Semana 8 — NIO.2, Maven, Gradle e checkpoint

## Resultado

Importar CSV com NIO.2, compreender builds reproduzíveis e entregar o checkpoint Java moderno `v0.1`.

Veja [roteiros](aulas.md). Execute `./scripts/test.sh`, `./scripts/test.sh --solution` e `./scripts/check-build-files.sh`.
