# Semana 10 — Networking, segurança e serialização

## Resultado

Consumir HTTP local com timeout/status, assinar payloads e preferir formatos interoperáveis a serialização nativa insegura.

Veja [roteiros](aulas.md). Valide com `./scripts/test.sh` e `./scripts/test.sh --solution`.
