# Semana 5 — Lambdas, interfaces funcionais e Optional

## Resultado

Transformar regras de catálogo em funções pequenas, compostas e testáveis, usando `Optional` somente para ausência legítima de retorno.

## Sequência

1. Lambdas, method references e interfaces padrão.
2. Interfaces funcionais próprias e composição.
3. `Optional`: operações e usos inadequados.
4. Motor declarativo de promoções.

Consulte [os quatro roteiros](aulas.md), implemente `PromotionEngine` e valide:

```bash
./scripts/test.sh
./scripts/test.sh --solution
```

**Entrega:** regras compostas, busca sem `null`, testes verdes, diário e rubrica.
