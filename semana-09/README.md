# Semana 9 — Reflection, annotations e annotation processing

## Resultado

Inspecionar metadados com segurança, ocultar campos sensíveis e compreender quando geração em compilação substitui reflexão em runtime.

Veja [roteiros](aulas.md). Valide com `./scripts/test.sh` e `./scripts/test.sh --solution`.
